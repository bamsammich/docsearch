# The proto lives in proto/, and the generated Go lands beside the code that
# uses it, under internal/api/ingestpb.
version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
