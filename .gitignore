# Build artifacts
/bin/
*.exe

# Runtime state — database, WAL sidecars, ingested library
/var/
*.db
*.db-wal
*.db-shm

# Local secrets
.env
.env.local

# Editor
.DS_Store
# Holds absolute paths to whatever is installed on one machine.
.vscode/
